# frc-mcp-remote — Cloudflare Worker
Remote (streamable HTTP) version of frc-mcp. Deploy: Cloudflare dashboard → Workers → Create → paste worker.js → Deploy.
Custom domain (optional): add mcp.financeratecalc.com in Worker settings once DNS is on Cloudflare.
Same 5 tools, same red line: no individual predictions, ever.
